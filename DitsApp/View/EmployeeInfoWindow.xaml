<Window x:Class="DitsApp.EmployeeInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DitsApp"
        mc:Ignorable="d"
        Title="EmployeeInfoWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1"
                  x:Name="EmployeeInfoDataGrid"
                  AutoGenerateColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Path=MaintenanceId}" Width="*"/>
                <DataGridTextColumn Header="MaintainerId" Binding="{Binding Path=MaintainerId}" Width="*"/>
                <DataGridTextColumn Header="EquipmentId" Binding="{Binding Path=EquipmentId}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Path=MaintenanceDate}" Width="*"/>
                <DataGridTextColumn Header="Next date" Binding="{Binding Path=DueDate}" Width="*"/>
                <DataGridTextColumn Header="Comment" Binding="{Binding Path=Comment}" Width="*"/>
            </DataGrid.Columns>
            
        </DataGrid>
        
    </Grid>
</Window>
